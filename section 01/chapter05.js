//1. Number Type

let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

// 2. String Type

let MyName = "박용규";
let myLocation = "목동";
let introduce = myName + myLocation;

//템플릿 리터럴 문법
let introduceText = `${myName}은 ${myLocation}에 거주합니다.`;

//3. boolean Type

let isSwitchOn = true;
let isEmpty = false;

//4. Null Type (아무것도 없다)
let empty = null;

//5. Undefined Type
let none;

//Undefined는 변수에 아무것도 할당을 하지 않았을 때 자동으로 들어가는 값이다.
//하지만 Null은 개발자가 직접 할당해줘야 하는 값이다.
//둘 다 비슷하긴 하지만 이런 차이점이 있다.

